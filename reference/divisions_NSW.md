# Allhomes NSW suburb (division) names and IDs

Allhomes NSW suburb (division) names and IDs

## Usage

``` r
data(divisions_NSW)
```

## Format

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with
2715 rows and 6 columns.
