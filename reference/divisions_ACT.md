# Allhomes ACT suburb (division) names and IDs

Allhomes ACT suburb (division) names and IDs

## Usage

``` r
data(divisions_ACT)
```

## Format

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with 126
rows and 6 columns.
